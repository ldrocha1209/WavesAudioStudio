{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the Waves Phase 0 window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
